name: PR Helper
on:
  pull_request:
    types: [opened, edited, reopened]
permissions:
  issues: write
  pull-requests: write
jobs:
  pr-greeting:
    runs-on: ubuntu-latest
    steps:
      - name: Create or update PR comment
        uses: peter-evans/create-or-update-comment@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            Hi @${{ github.event.pull_request.user.login }} — thanks for the PR! 🍝
            
            **Quick reminders:**
            - Target `develop` for features, `main` for hotfixes
            - Run `dotnet test` locally (all tests should pass ✅)
            - Run `dotnet format` (code should be formatted ✨)
            - Add testing steps to PR description
            - Update `CHANGELOG.md` if needed
            
            ---
            🤖 *Savory Backend Bot*
